# Function

```java
class User {
    // fetch ID embedded in the form
    createAccount (user_name, password);
    login(user_name, password);
    logout ();
    register_as_driver (driver_name,
                        vehicle_type,
                        license_number,
                        capacity,
                        other_info);
}
```

```java
class Ride {
    search_sharer ();
    request_a_ride ();
    
}
```

